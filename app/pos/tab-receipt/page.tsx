// This route is no longer used. Tab settlements are rendered using the
// existing /pos/receipt/[orderId] receipt layout via a consolidated order.
export default function DeprecatedTabReceiptPage() {
  return null;
}
